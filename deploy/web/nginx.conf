server {
  listen 80;
  server_name _;
  client_max_body_size 22m;

  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "SAMEORIGIN" always;
  add_header Referrer-Policy "strict-origin-when-cross-origin" always;

  # app 端接口(/api,含 portal 开放接口)
  location /api/ {
    proxy_pass http://api:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # admin 端接口(/admin-api)
  location /admin-api/ {
    proxy_pass http://api:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # 公开/私有文件下载
  location /files/ {
    proxy_pass http://api:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # admin 端带 hash 的静态资源:内容即版本,长缓存
  location /admin/assets/ {
    alias /usr/share/nginx/admin/assets/;
    expires 1y;
    add_header Cache-Control "public, immutable";
  }

  # admin 管理端 SPA(构建时 base=/admin/);index 必须 no-cache,
  # 否则发版后浏览器停在旧 bundle(用 expires 而非 add_header,避免丢失 server 级安全头)
  location /admin/ {
    alias /usr/share/nginx/admin/;
    expires -1;
    try_files $uri $uri/ /admin/index.html;
  }

  # app 端 SPA 资源(根路径)
  location /assets/ {
    try_files $uri =404;
    expires 1y;
    add_header Cache-Control "public, immutable";
  }

  # app 端 SPA(根路径,history 路由刷新回退 index.html);同样 index no-cache
  location / {
    root /usr/share/nginx/html;
    expires -1;
    try_files $uri $uri/ /index.html;
  }
}
